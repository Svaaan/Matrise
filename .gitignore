# Single-file build (-Bundle)
Matrise.bundle.ps1

# Runtime state - written by the app on the machine it runs on, not source.
# named/known devices, per machine
devices.json
# remembered window size/position
matrise-layout.json
# tamper-evident audit log + startup/ui error logs
audit/
# saved console/command output
reports/
